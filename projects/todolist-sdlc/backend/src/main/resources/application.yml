server:
  port: 8080

spring:
  application:
    name: todolist-sdlc

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # MySQL 配置（与 database-migrations/pom.xml 保持一致）
    url: jdbc:mysql://127.0.0.1:13306/todolist?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true&rewriteBatchedStatements=true&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root@P@SSw0Rd

  flyway:
    enabled: false  # 默认禁用，仅在 dev 模式启用
    locations: classpath:db/migration,filesystem:database-migrations/migrations
    baseline-on-migrate: true

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: todolist-sdlc-secret-key-for-jwt-token-generation-must-be-long-enough
  expiration: 86400000

logging:
  level:
    com.todolist: debug
    org.springframework.security: info
