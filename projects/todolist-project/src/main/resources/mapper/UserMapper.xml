<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.todolist.mapper.UserMapper">

    <!-- Select user by username -->
    <select id="selectByUsername" resultType="com.todolist.domain.entity.User">
        SELECT id, username, password, locked, locked_at, last_login_at, created_at, updated_at, deleted
        FROM sys_user
        WHERE username = #{username}
          AND deleted = 0
    </select>

    <!-- Check if username exists -->
    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM sys_user
        WHERE username = #{username}
          AND deleted = 0
    </select>

</mapper>
